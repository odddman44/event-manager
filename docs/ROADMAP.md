# 이벤트 매니저 MVP 개발 로드맵

주최자가 이벤트를 만들고 링크를 공유하면 참여자가 회원가입 없이 즉시 참여 의사를 밝힐 수 있는 서비스

## 현재 상태

- **진행 단계**: Phase 0 (프로젝트 셋업) 시작 전
- **최종 업데이트**: 2026-06-28

## 개요

이벤트 매니저는 소규모 이벤트를 반복적으로 주최하는 개인 주최자와 그 참여자를 위한 서비스로, 카카오톡 기반의 비효율적 참여자 관리를 대체합니다. 핵심 가치는 다음과 같습니다:

- **간편한 이벤트 생성**: 제목/날짜/장소/정원만 입력하면 공유 링크(share_token) 자동 발급
- **회원가입 없는 참여**: 참여자는 링크 클릭 후 이름/메모만으로 즉시 참여, guest_token으로 재방문 인식
- **실시간 참여 현황 관리**: 주최자는 한 페이지에서 참여자 목록 확인 및 링크 배포

## 기술 스택

- **프론트엔드**: Next.js 15 (App Router), TypeScript 5.6+, React 19
- **스타일링/UI**: TailwindCSS v4, shadcn/ui, Lucide React (럭키밀 스타일, 포인트 컬러 `#FF6B35`)
- **폼/검증**: React Hook Form 7.x, Zod
- **백엔드/DB**: Supabase (Auth, PostgreSQL, RLS)
- **아키텍처**: Controller(Server Action/Route Handler) → Service → Repository (레이어드)
- **배포**: Vercel

## 개발 워크플로우

1. **작업 계획**: 기존 코드베이스 상태를 파악하고 `ROADMAP.md`를 갱신하여 우선순위 작업을 결정
2. **작업 구현**: 각 Task의 체크리스트를 따라 구현하고, 단계 완료 시마다 진행 상황 갱신
3. **테스트 검증**: API 연동 및 비즈니스 로직 구현 시 Playwright MCP로 E2E 테스트 수행 후 다음 단계 진행
4. **로드맵 업데이트**: 완료된 Task와 세부 항목을 ✅로 표시

## 상태 표시 규칙

- **✅**: 완료된 Task / 세부 항목
- **- 우선순위**: 즉시 시작해야 할 Task
- **[ ]**: 미완료 세부 항목 / **[x]**: 완료된 세부 항목

---

## 개발 단계

### Phase 0: 프로젝트 셋업

> 환경 설정, Supabase 연동, DB 마이그레이션 — 모든 후속 작업의 기반

- **Task 001: Next.js 프로젝트 초기화 및 기본 환경 구성** - 우선순위
  - [ ] Next.js 15 (App Router) + TypeScript + React 19 프로젝트 셋업
  - [ ] TailwindCSS v4 설정 및 럭키밀 디자인 토큰 정의 (포인트 컬러 `#FF6B35`, border-radius 16-24px)
  - [ ] shadcn/ui 초기화 및 Lucide React 설치
  - [ ] ESLint/Prettier, 절대 경로(`@/*`) 등 개발 환경 설정
  - [ ] 레이어드 아키텍처 디렉토리 구조 생성 (`controllers`/`services`/`repositories`/`lib`/`types`)

- **Task 002: Supabase 프로젝트 연동 및 클라이언트 설정**
  - [ ] Supabase 프로젝트 생성 및 환경 변수(`.env.local`) 구성
  - [ ] 서버/클라이언트용 Supabase 클라이언트 분리 설정 (`@supabase/ssr`)
  - [ ] 연결 검증용 헬스 체크 수행

- **Task 003: 데이터베이스 스키마 및 RLS 마이그레이션**
  - [ ] `profiles` 테이블 마이그레이션 (id, name, role, created_at)
  - [ ] `events` 테이블 마이그레이션 (organizer_id, title, description, event_date, location, max_participants, share_token UNIQUE, created_at)
  - [ ] `participants` 테이블 마이그레이션 (event_id, name, memo, guest_token UNIQUE, status, created_at)
  - [ ] RLS 정책 설정: events(주최자 본인 CRUD, share_token SELECT 공개)
  - [ ] RLS 정책 설정: participants(guest_token 소유자 UPDATE, 주최자 전체 SELECT)
  - [ ] 회원가입 시 `profiles` 자동 생성 트리거 작성
  - [ ] `supabase gen types`로 TypeScript 타입 생성

- **Task 004: 공통 타입 정의 및 라우트 골격 구성**
  - [ ] DB 스키마 기반 도메인 타입 및 DTO 정의 (`Profile`, `Event`, `Participant`)
  - [ ] Zod 검증 스키마 정의 (이벤트 생성, 참여 신청, 인증 폼)
  - [ ] App Router 전체 라우트 빈 페이지 스캐폴딩 (`/`, `/auth/login`, `/auth/signup`, `/dashboard`, `/events/new`, `/events/[id]`, `/join/[share_token]`, `/admin`, `/admin/events`, `/admin/users`)
  - [ ] 공통 레이아웃 및 네비게이션 골격 구현

### Phase 1: 인증 시스템 (F010)

> 회원가입 / 로그인 / 로그아웃 및 접근 제어 미들웨어

- **Task 005: 회원가입 / 로그인 / 로그아웃 구현 (F010)**
  - [ ] 회원가입 페이지 — 이름/이메일/비밀번호 폼, Supabase Auth 계정 생성 (role 기본값 user)
  - [ ] 로그인 페이지 — 이메일/비밀번호 폼, 로그인 처리 및 에러 메시지 표시
  - [ ] 로그아웃 처리 및 랜딩 페이지 리다이렉트
  - [ ] 랜딩 페이지 — 서비스 소개 + [로그인]/[시작하기] 진입 버튼
  - [ ] role 기반 로그인 후 리다이렉트 (admin → `/admin`, user → `/dashboard`)
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] 회원가입 → 자동 로그인 → 내 이벤트 목록 이동 플로우
    - [ ] 로그인 성공/실패(잘못된 비밀번호, 미가입 계정) 시나리오
    - [ ] 로그아웃 후 보호 경로 접근 차단 확인

- **Task 006: 접근 제어 미들웨어 구현**
  - [ ] `/admin/*` — admin role 확인, 아니면 `/dashboard` 리다이렉트
  - [ ] `/dashboard`, `/events/*` — 로그인 확인, 아니면 `/auth/login` 리다이렉트
  - [ ] `/join/*` 및 인증 페이지 — 공개/비로그인 전용 처리
  - [ ] 비로그인 전용 페이지(로그인/회원가입)에 로그인 상태로 접근 시 리다이렉트
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] user 계정으로 `/admin` 접근 시 `/dashboard` 리다이렉트 확인
    - [ ] 비로그인 상태로 `/dashboard` 접근 시 `/auth/login` 리다이렉트 확인

### Phase 2: 주최자 기능 (F001, F002, F003)

> 이벤트 생성, 내 이벤트 목록, 이벤트 관리

- **Task 007: 이벤트 생성 (F001)**
  - [ ] 이벤트 생성 페이지 UI — 제목(필수)/날짜시간(필수)/장소(선택)/정원(선택) 폼
  - [ ] React Hook Form + Zod 검증 적용
  - [ ] Server Action → Service → Repository 흐름으로 이벤트 등록 및 `share_token` 자동 발급
  - [ ] 생성 성공 시 이벤트 관리 페이지로 이동, 실패 시 에러 메시지 표시
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] 필수값 입력 후 생성 → 관리 페이지 이동 및 share_token 발급 확인
    - [ ] 필수값 누락 시 검증 에러 표시 확인

- **Task 008: 내 이벤트 목록 조회 (F002)**
  - [ ] 내 이벤트 목록 페이지 — 본인 이벤트 카드 리스트 (제목/날짜/장소/참여자 수 / 정원)
  - [ ] 이벤트 없을 때 빈 상태 안내 문구
  - [ ] [새 이벤트 만들기] 버튼 및 카드 클릭 → 관리 페이지 이동
  - [ ] 모바일 카드 리스트 반응형 레이아웃 (max-width 430px 기준)
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] 이벤트 생성 후 목록에 반영 확인
    - [ ] 빈 상태 안내 노출 확인

- **Task 009: 이벤트 관리 (F003)**
  - [ ] 이벤트 정보 요약 표시 (제목/날짜/장소/정원)
  - [ ] 공유 링크 표시 + [링크 복사] 버튼 (클립보드 복사)
  - [ ] 참여자 목록 테이블 (이름/메모/참여 상태/신청 시각)
  - [ ] 참여 인원 / 정원 카운터
  - [ ] [내 이벤트 목록으로] 링크 및 주최자 본인 검증
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] 링크 복사 동작 및 참여자 목록 표시 확인
    - [ ] 타 사용자의 이벤트 관리 페이지 접근 차단 확인

### Phase 3: 참여자 기능 (F004, F005)

> 비회원 참여 신청 및 참여 상태 변경

- **Task 010: 비회원 참여 신청 (F004)**
  - [ ] 참여 페이지(`/join/[share_token]`) — 이벤트 정보 표시 (제목/날짜/장소/참여자 수 / 정원)
  - [ ] 신규 방문 시 이름(필수)+메모(선택) 입력 폼 + [참여하기] 버튼
  - [ ] 참여 등록 후 `guest_token` 발급 및 localStorage 저장
  - [ ] 정원 초과 시 참여 불가 안내 메시지
  - [ ] 참여 완료 메시지 표시 (싱글 페이지, 스크롤 없는 한 화면 구성)
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] 신규 참여 → guest_token localStorage 저장 및 완료 메시지 확인
    - [ ] 정원 초과 시 참여 불가 처리 확인
    - [ ] 유효하지 않은 share_token 접근 시 에러 처리 확인

- **Task 011: 참여 상태 변경 (F005)**
  - [ ] 재방문 시 localStorage의 guest_token으로 기존 참여 자동 인식
  - [ ] 이름 표시 + 메모 수정 필드 + [메모 저장] 버튼
  - [ ] [참여 취소] 버튼 → status를 cancelled로 변경, 취소 완료 메시지 표시
  - [ ] guest_token 소유자만 본인 행 UPDATE (RLS 검증)
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] 재방문 시 기존 참여 자동 인식 확인
    - [ ] 메모 수정 저장 반영 확인
    - [ ] 참여 취소 후 상태 변경 및 주최자 화면 반영 확인

- **Task 011-1: 주최자-참여자 통합 플로우 테스트**
  - [ ] 이벤트 생성 → 링크 공유 → 참여 등록 → 관리 페이지 참여 현황 반영 전체 플로우 검증 (Playwright MCP)
  - [ ] 참여/취소에 따른 참여 인원 카운터 정합성 검증
  - [ ] 에러 핸들링 및 엣지 케이스(정원 경계, 중복 참여) 검증

### Phase 4: 어드민 기능 (F006, F007, F008)

> 플랫폼 통계, 전체 이벤트 관리, 전체 사용자 관리

- **Task 012: 플랫폼 통계 조회 (F006)**
  - [ ] 어드민 대시보드 — 통계 카드 (총 가입자 수 / 총 이벤트 수 / 총 참여자 수)
  - [ ] 어드민 이벤트/사용자 목록 페이지 링크 및 로그아웃 버튼
  - [ ] 사이드바 + 콘텐츠 데스크톱 레이아웃 (min-width 1024px 기준)
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] admin 계정 통계 수치 정확성 확인
    - [ ] user 계정 접근 차단 확인

- **Task 013: 전체 이벤트 관리 (F007)**
  - [ ] 전체 이벤트 테이블 (제목/주최자/날짜/참여자 수/생성일)
  - [ ] 이벤트별 [삭제] 버튼 + 삭제 확인 다이얼로그
  - [ ] 삭제 후 목록 갱신 및 어드민 대시보드 링크
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] 이벤트 삭제 → 목록 갱신 및 연관 참여자 처리 확인
    - [ ] 삭제 확인 다이얼로그 취소 동작 확인

- **Task 014: 전체 사용자 관리 (F008)**
  - [ ] 전체 사용자 테이블 (이름/이메일/역할/가입일)
  - [ ] 사용자별 [삭제] 버튼 + 삭제 확인 다이얼로그
  - [ ] 삭제 후 목록 갱신 및 어드민 대시보드 링크
  - **## 테스트 체크리스트** (Playwright MCP)
    - [ ] 사용자 삭제 → 목록 갱신 확인
    - [ ] 삭제 확인 다이얼로그 취소 동작 확인

### Phase 5: 배포 및 최종 점검

> 품질 보증, 성능 점검, Vercel 배포

- **Task 015: 전체 사용자 플로우 E2E 통합 테스트**
  - [ ] 주최자/참여자/어드민 3개 흐름 전체 시나리오 E2E 테스트 (Playwright MCP)
  - [ ] 인증/권한 경계 및 RLS 정책 종합 검증
  - [ ] 반응형(어드민 데스크톱 / 주최자·참여자 모바일) 동작 확인

- **Task 016: Vercel 배포 및 운영 준비**
  - [ ] Vercel 프로젝트 연결 및 환경 변수 설정
  - [ ] Supabase 프로덕션 URL/리다이렉트 설정 및 RLS 최종 점검
  - [ ] 프로덕션 빌드 검증 및 배포
  - [ ] 배포 환경 스모크 테스트 (회원가입 → 이벤트 생성 → 참여)
  - [ ] Supabase advisors(보안/성능) 점검 및 정리
